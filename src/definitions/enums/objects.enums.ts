export enum ObjectType {
  Group = 'group',
  Model = 'model',
  Gltf = 'gltf',
  Box = 'box',
  Line = 'line',
}

export enum ObjectSource {
  Hosted = 'hosted',
  External = 'external',
}

export enum EntityCollision {
  static = 'static',
  kinematic = 'kinematic',
  dynamic = 'dynamic',
}
