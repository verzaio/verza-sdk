export type PlayerControls = {
  // movement
  forward: boolean;
  left: boolean;
  backward: boolean;
  right: boolean;

  jump: boolean;
  sprint: boolean;

  // others
  caps: boolean;
  alt: boolean;
  control: boolean;
};
